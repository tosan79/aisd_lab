#!/usr/bin/perl -l

srand (1007);

$n = 100000;
$q = 100000;

print "$n $q";
for (0..4) { $leaf{$_} = 1; };
for (2..$n) {
	$x = int(rand(5));
	print $leaf{$x}; 
	$leaf{$x} = $_; 
}

for (1..$q) {
	$a = $b = int (rand($n)) + 1;
	while ($a == $b) {
		$b = int (rand($n)) + 1;
	}
	print "$a $b";
}

