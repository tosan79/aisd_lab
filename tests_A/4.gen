#!/usr/bin/perl -l

srand (1007);

$n = 1000000;
$q = 100000;

print "$n $q";
for (2..$n) {
	$x = int($_/1.001);
	print ($x >= 1 ? $x : 1);
}

for (1..$q) {
	$a = $b = int (rand($n)) + 1;
	while ($a == $b) {
		$b = int (rand($n)) + 1;
	}
	print "$a $b";
}

